.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
secret.json
logs/
*.lock

# Echte Bitget-Kontoauszuege (Order-/Positions-/Transaktionshistorie, Account-ID sichtbar) --
# nie ins Repo, nur lokal fuer Live-vs-Backtest-Abgleich.
livetrades/
daten/

# Generierte Charts -- koennen gross werden, jederzeit neu erzeugbar
artifacts/charts/

# Grosse Trainingsdaten/Caches (100MB+ pro Datei) -- nicht fuer Git geeignet.
artifacts/datasets/*.jsonl
artifacts/datasets/ohlcv_*.pkl
artifacts/datasets/renko_ohlcv_*.pkl

# Dasselbe fuer den taeglichen Renko-Konsistenzcheck (analysis/renko_live_signal_check.py).
artifacts/datasets/last_renko_signal_check_run.txt

# Live-Zustand der Anti-Martingale-Positionsgroesse (siehe anti_martingale.py) -- rein
# runtime-generiert, pro Account unterschiedlich, nicht fuer Git geeignet.
artifacts/state/
