# oraclebot requirements
# Exchange
ccxt==4.3.5

# Telegram-Benachrichtigungen
requests

# Data
numpy==2.3.5
# pandas 2.1.3 (der alte Pin) predates numpy 2.0 und ist mit numpy==2.3.5 inkompatibel
# (pip ResolutionImpossible auf dem VPS 2026-07-13) -- 2.3.3 ist die tatsaechlich lokal
# genutzte, mit numpy 2.3.5 getestete Version.
pandas==2.3.3

# Tests
pytest

# Charts/Reports (analysis/renko_chart.py, Excel/HTML-Exports) -- ohne sie brechen diese Skripte
# auf einer frischen Installation (z.B. VPS) mit "ModuleNotFoundError" ab.
plotly>=6.0.0
openpyxl>=3.1.0

# Echtzeit-Ausfuehrung (scripts/run_renko_realtime.py) -- roher WebSocket-Client fuer Bitgets
# oeffentlichen Trade-Kanal, da ccxt.pro nicht installiert/lizenziert ist.
websockets>=13.0
