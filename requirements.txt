# oraclebot requirements
# Exchange
ccxt==4.3.5

# Telegram-Benachrichtigungen
requests
matplotlib

# Data
# EXAKT gepinnt (nicht nur unversioniert): RandomForest/HistGBM-Modelle picklen intern einen
# numpy BitGenerator-RNG-Zustand, dessen Format sich zwischen numpy-Versionen aendert -- ein
# Versions-Mismatch zwischen Trainings- und Inferenz-Umgebung fuehrt zu "PCG64 is not a known
# BitGenerator module" beim Laden von tree_ensemble.pkl (VPS 2026-07-13, derselbe Fehlertyp wie
# schon beim sklearn-Pin unten).
numpy==2.3.5
# pandas 2.1.3 (der alte Pin) predates numpy 2.0 und ist mit numpy==2.3.5 inkompatibel
# (pip ResolutionImpossible auf dem VPS 2026-07-13) -- 2.3.3 ist die tatsaechlich lokal
# genutzte, mit numpy 2.3.5 getestete Version.
pandas==2.3.3

# Technical Indicators
ta==0.11.0

# Machine Learning
# scikit-learn EXAKT gepinnt (nicht >=): tree_ensemble.pkl wurde mit dieser Version trainiert
# und gepickelt -- sklearn warnt explizit vor "breaking code or invalid results" bei
# Versions-Mismatch (beobachtet 2026-07-10: VPS installierte per >=1.3.0 automatisch die
# neueste 1.9.0 statt der Trainings-Version 1.8.0, InconsistentVersionWarning bei jedem Lauf).
scikit-learn==1.8.0
torch>=2.1.0

# Tests
pytest
